export interface RentalSimple {
  id: string;
  rentalDate: Date;
  durationInSeconds: number;
  clientName: string;
}
export interface Rental {
  id: string;
  rentalDate: Date;
  durationInSeconds: number;
  clientName: string;
}
